const baseURL = "http://localhost:8080/api/authentication";

export const LOGIN_URL = `${baseURL}/login`;
export const LOGOUT_URL = `${baseURL}/logout`;
export const REGISTRATION_URL = `${baseURL}/registration`;
